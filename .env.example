# =====================================================
# KIREIDOT Salonboard Worker
# =====================================================
# KIREIDOT Admin の URL
# ローカル開発: http://localhost:3000
# 本番: https://<your-vercel-domain>
KIREIDOT_API_URL=http://localhost:3000

# 実行モード
#   device      … 本番。Admin で発行された device_id / device_token を使う。
#   central-dev … 開発専用 (Admin が NODE_ENV!='production' のときだけ通る)。
WORKER_MODE=device

# --- device モード用 (WORKER_MODE=device) ---
# Admin の "/admin/salonboard/devices" 画面で発行された値を貼る。
# device_token は発行時に1度だけ表示される平文トークン。
SALONBOARD_DEVICE_ID=
SALONBOARD_DEVICE_TOKEN=

# --- central-dev モード用 (WORKER_MODE=central-dev) ---
# KIREIDOT_Admin/.env.local の SALONBOARD_WORKER_TOKEN と完全一致させること。
SALONBOARD_WORKER_TOKEN=

# 任意のワーカー識別子 (ログや job.locked_by に使われる)
WORKER_ID=local-dev-01

# Worker のアプリバージョン (省略時は package.json から自動取得)
# APP_VERSION=0.1.29

# ポーリング間隔 (ミリ秒)。デフォルト 30秒
POLL_INTERVAL_MS=30000

# ドライラン: true にすると Playwright は起動せず、ジョブを即 succeeded で callback する
# (push_booking は DRY_RUN では manual_required で安全に止める)
DRY_RUN=false

# push_booking の実登録スイッチ (安全装置)。
#   有効値は "1" または "true" (大文字小文字無視) のみ。
#     1 / true       : 確認画面の照合が一致したときのみ自動で登録ボタンを押す。
#     未設定/空/その他 : すべて無効。確認画面まで進めて payload と照合するが
#                       「登録」ボタンは押さず、Admin 側で manual_required になる
#                       (= dryRun 相当の安全モード)。
# 本番 SalonBoard にいきなり登録させないため、明示的に有効化するまで false 推奨。
SALONBOARD_ENABLE_PUSH=false

# 予約登録フローで失敗したときに実 DOM 調査用の debug capture を保存するか。
#   既定 ON。0/false/no で無効化。
#   保存先: ~/.kireidot/salonboard-debug/push_booking/{時刻}_{job8}_{ラベル}/
#   個人情報・パスワードはマスク/除外して保存する (input value は保存しない)。
SALONBOARD_DEBUG_CAPTURE=1

# =====================================================
# KIREIDOT サロンデスク (Electron + Vite) 用
# Vite では VITE_* プレフィックスのみブラウザ側に露出する
# =====================================================
VITE_SUPABASE_URL=https://cxbqbjrxsuuabhxlrdpz.supabase.co
VITE_SUPABASE_ANON_KEY=

# 同一Cloud出口からSalonBoardへ自動ログインする最小間隔(ms)。
# ログイン済みジョブには適用されず、AkamaiのdoLogin集中を防ぐためだけに使います。
SB_LOGIN_MIN_INTERVAL_MS=20000
VITE_APP_NAME=KIREIDOT サロンデスク

# Admin API ベース URL (例: http://localhost:3000 / https://admin.kireidot.jp)
# 旧 RPC 直叩きをやめ、credentials は /api/salonboard/device/credentials 経由で取得する。
VITE_KIREIDOT_API_URL=http://localhost:3000

# 店舗 PC ごとの device 認証情報。Admin の /admin/salonboard/devices で発行する。
# Worker (Electron) はこれを使って credentials を Admin API から取得する。
VITE_SALONBOARD_DEVICE_ID=
VITE_SALONBOARD_DEVICE_TOKEN=

# 任意のラベル (job.locked_by や Admin ログに残る)
VITE_WORKER_ID=electron-worker
# 任意 (省略時は package.json から自動取得)
# VITE_APP_VERSION=0.2.2

# =====================================================
# macOS Notarization (Apple 公証)
# 推奨: Keychain profile 方式
#   ターミナルで一度だけ:
#     xcrun notarytool store-credentials "予約同期くん-notary" \
#       --apple-id <YOUR_APPLE_ID> \
#       --team-id 7FMVQPBJKA \
#       --password <APP-SPECIFIC-PASSWORD>
#   その後、ここに profile 名だけを書く (秘密情報は Keychain 内)
APPLE_NOTARY_KEYCHAIN_PROFILE=予約同期くん-notary

# 環境変数方式 (Keychain が使えない CI 等で使用)
# 上記の APPLE_NOTARY_KEYCHAIN_PROFILE が設定されていない場合のフォールバック。
# 実値は絶対に Git に含めない。
# APPLE_ID=
# APPLE_APP_SPECIFIC_PASSWORD=
# APPLE_TEAM_ID=7FMVQPBJKA

# 公証をスキップしたい場合 (社内開発向けのテスト dmg 作成)
# SKIP_NOTARIZE=1
